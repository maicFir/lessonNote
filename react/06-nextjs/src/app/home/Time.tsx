/**
 * @description 请添加组件描述
 * @author maicFir
 */
import React, { memo } from "react";

type Props = object;

const Time: React.FC<Props> = (props) => {
  const {} = props;
  return <div>Time</div>;
};

export default memo(Time);
