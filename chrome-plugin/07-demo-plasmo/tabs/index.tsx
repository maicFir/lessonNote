/**
 * @description 请添加组件描述
 * @author maicFir
 */
import React, { memo } from "react"

interface Props {}

const Index: React.FC<Props> = (props) => {
  const {} = props
  return <div>newtab index</div>
}

export default memo(Index)
